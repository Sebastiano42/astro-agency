---
import Layout from '../../layouts/Layout.astro';

const features = [
  { icon: '⚡', title: 'LCP', desc: 'Immagini e testi principali visibili in pochi millisecondi.' },
  { icon: '👆', title: 'FID', desc: 'Interattività immediata al primo tocco dell\'utente.' },
  { icon: '📐', title: 'CLS', desc: 'Stabilità visiva totale senza salti di layout.' },
  { icon: '🖼️', title: 'Image Engine', desc: 'Compressione intelligente senza perdita di qualità.' }
];

const metrics = [
  { label: 'Performance', value: 100 },
  { label: 'Accessibilità', value: 100 },
  { label: 'Best Practices', value: 100 },
  { label: 'SEO', value: 100 }
];

const serviceSchema = {
  "@context": "https://schema.org",
  "@type": "Service",
  "serviceType": "Web Performance Optimization",
  "provider": {
    "@type": "Person",
    "name": "Sebastiano Moniaci"
  },
  "areaServed": {
    "@type": "Country",
    "name": "Italy"
  },
  "description": "Ottimizzazione Performance Web: Core Web Vitals perfetti, Lighthouse score 100/100. Compressione immagini, code splitting e tempi di caricamento sotto i 2 secondi.",
  "offers": {
    "@type": "Offer",
    "availability": "https://schema.org/InStock"
  }
};
---

<Layout
  title="Performance Optimization"
  description="Ottimizzazione Performance Web: Core Web Vitals perfetti, Lighthouse score 100/100. Compressione immagini, code splitting e tempi di caricamento sotto i 2 secondi."
>
  <script type="application/ld+json" set:html={JSON.stringify(serviceSchema)} slot="head" />
  <!-- Hero Section -->
  <section class="pt-32 pb-20 md:pt-48 md:pb-32 container-custom">
    <a href="/servizi" class="inline-flex items-center gap-2 text-gray-500 hover:text-white transition-colors mb-8 text-sm font-mono uppercase tracking-widest reveal">
      <svg width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
        <path d="M19 12H5M12 19l-7-7 7-7"/>
      </svg>
      Torna ai servizi
    </a>
    
    <div class="grid lg:grid-cols-2 gap-16 items-center">
      <div>
        <span class="inline-block px-4 py-2 rounded-full bg-violet-500/10 border border-violet-500/20 text-violet-400 text-xs font-mono uppercase tracking-widest mb-6 reveal">
          Speed Matters
        </span>
        <h1 class="text-4xl sm:text-5xl md:text-7xl lg:text-8xl font-black text-white mb-8 tracking-tight leading-none reveal-scale">
          Velocità<br/><span class="text-gray-600">Record.</span>
        </h1>
        <p class="text-lg md:text-xl text-gray-400 leading-relaxed max-w-xl reveal">
          Un sito lento fa perdere clienti. Ottimizzo ogni byte per caricamenti istantanei.
        </p>
      </div>
      
      <div class="reveal-scale mockup-float-wrapper">
        <!-- Lighthouse score -->
        <div class="mockup-float relative flex items-center justify-center">
          <div class="absolute inset-0 bg-gradient-to-br from-green-500/20 to-emerald-500/10 rounded-full blur-3xl"></div>
          
          <div class="relative w-72 h-72 rounded-full border-8 border-green-500/30 flex flex-col items-center justify-center bg-gradient-to-br from-white/[0.08] to-transparent">
            <div class="absolute inset-2 rounded-full border-4 border-green-500 animate-pulse"></div>
            
            <span class="text-8xl font-black text-green-500 relative">100</span>
            <span class="text-sm font-mono text-gray-400 mt-2">Lighthouse Score</span>
          </div>
        </div>
        
        <div class="grid grid-cols-4 gap-4 mt-8">
          {metrics.map((metric) => (
            <div class="text-center">
              <div class="w-12 h-12 rounded-full bg-green-500/20 border border-green-500/30 flex items-center justify-center mx-auto mb-2">
                <span class="text-green-500 font-bold text-sm">{metric.value}</span>
              </div>
              <span class="text-[10px] font-mono text-gray-500 uppercase tracking-wider">{metric.label}</span>
            </div>
          ))}
        </div>
      </div>
    </div>
  </section>

  <!-- Features -->
  <section class="py-24 bg-gradient-to-b from-surface/50 to-background border-y border-white/5">
    <div class="container-custom">
      <h2 class="text-3xl md:text-4xl font-black text-white mb-12 reveal-scale">Core Web Vitals</h2>
      
      <div class="grid md:grid-cols-2 lg:grid-cols-4 gap-6">
        {features.map((feature, index) => (
          <div class={`group reveal-scale delay-${(index + 1) * 100}`}>
            <div class="glow-border relative h-full bg-gradient-to-br from-white/[0.08] to-transparent border border-white/10 rounded-2xl p-6 transition-all duration-500 hover:border-white/30 hover:bg-white/[0.05] overflow-hidden">
              <div class="absolute -top-16 -right-16 w-32 h-32 bg-gradient-to-br from-violet-500/20 to-indigo-500/10 rounded-full blur-3xl opacity-0 group-hover:opacity-100 transition-opacity duration-700"></div>
              
              <span class="text-4xl mb-6 block relative">{feature.icon}</span>
              <h3 class="text-xl font-bold text-white mb-3 group-hover:translate-x-1 transition-transform relative">{feature.title}</h3>
              <p class="font-mono text-sm text-gray-500 group-hover:text-gray-400 transition-colors relative">{feature.desc}</p>
              
              <div class="absolute bottom-0 left-0 right-0 h-[2px] bg-gradient-to-r from-transparent via-violet-500/50 to-transparent scale-x-0 group-hover:scale-x-100 transition-transform duration-500"></div>
            </div>
          </div>
        ))}
      </div>
    </div>
  </section>

  <!-- CTA -->
  <section class="py-32 relative overflow-hidden">
    <div class="absolute inset-0 bg-[radial-gradient(ellipse_80%_60%_at_50%_50%,_var(--tw-gradient-stops))] from-violet-500/5 via-violet-500/2 via-30% via-transparent via-60% to-transparent"></div>
    <div class="container-custom relative z-10">
      <div class="max-w-2xl mx-auto text-center">
        <h2 class="text-4xl md:text-5xl font-black text-white mb-6 reveal-scale">Il tuo sito<br/>è troppo lento?</h2>
        <p class="text-gray-400 mb-10 text-lg reveal">Scopri come velocizzare il tuo sito fino al 300%.</p>
        <a href="/contatti" class="btn-primary reveal">
          Testa la tua velocità
        </a>
      </div>
    </div>
  </section>
</Layout>
